# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SmallGMaps' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SmallGMaps
pod 'GoogleMaps' 
pod 'SwiftyJSON'
pod 'Alamofire', '~> 4.0'
end
